<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:itemBackground">@color/colorAccent</item>
        <item name="android:textColor">@color/DialogeTxt</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
    <style name="MyAlertstyle" parent="Theme.AppCompat.Dialog.Alert">
        <item name="colorAccent">@color/Dialoge</item>
        <item name="android:textColor">@color/DialogeTxt</item>
    </style>

    <style name="MyAlertstyle." parent="Theme.AppCompat.Dialog.Alert">
        <item name="android:textColor">@color/DialogeTxt</item>
        <item name="android:textColorPrimary">@color/DialogeTxt</item>
        <item name="colorAccent">@color/DialogeTxt</item>
    </style>

    <style name="MyAlertstyle2" parent="Theme.AppCompat.Dialog.Alert">
        <item name="android:textColor">@color/DialogeTxt</item>
        <item name="android:background">@color/colorAccent</item>
        <item name="android:textColorPrimary">@color/DialogeTxt</item>
        <item name="colorAccent">#FFFFFF</item>

    </style>
</resources>
